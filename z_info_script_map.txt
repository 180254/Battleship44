utils.random_string(length, chars)
utils.url_param(name)
utils.get_url(id)
utils.set(el_id, value)
utils.increment(el_id)

title.standard
title._blink_timeout
title._blink_interval
title.set(new_title)
title.set_blink(new_title, override)

events.on($e, action, callback)
events.on_onetime($e, action, callback)

clazz.cell.unknown
clazz.cell.empty
clazz.cell.ship
clazz.cell.opp_shoot:
clazz.msg.fail
clazz.msg.important
clazz.inactive

i18n.strings
i18n.supported
i18n.get_lang()
i18n.set_lang(lang)
i18n.init(callback)

grid.shoot
grid.opponent
grid.fresh(grid_id, rows_no, cols_no)
grid._new_row(grid_id, row_i, cols_no)
grid._new_cell(grid_id, row_i, col_i)
grid.set_cell(grid_id, row_i, col_i, new_class, remove_old_class)
grid.reset_both()

ship_selection.activate()
ship_selection.deactivate()
ship_selection._collect()
ship_selection.move()

serializer.cell_serialize(row_i, col_i)
serializer.cell_deserialize(cell)
serializer.cells_deserialize(cells)

message.msg_div
message.msg_const
message.timeout.fast
message.timeout.default
message.timeout.fast
message.set(message, timeout, clazz)
message.append_link(text, id)

game.ok_next_game
game.ok_ship_selection
game.init()

info.game_url
info.players_game
info.players_global
info.winning_me
info.winning_opp
info.stat[...]

ws._ws
ws.init()
ws.send()

err.translate
on_event_actions[...]
on_msg_actions[...]